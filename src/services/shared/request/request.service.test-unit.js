
describe('GET Request', () => {
  beforeAll(() => { });

  afterAll(() => { });

  beforeEach(() => { });

  afterEach(() => { });

  test.skip('can calculate 2 plus 2', () => {
    expect(2 + 2).toBe(4);
  });
});
