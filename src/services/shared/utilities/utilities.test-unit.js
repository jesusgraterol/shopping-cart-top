import Utilities from "./utilities.js";

describe('Utilities', () => {
  beforeAll(() => { });

  afterAll(() => { });

  beforeEach(() => { });

  afterEach(() => { });

  test.skip('can calculate 2 plus 2', async() => {
    expect(2 + 2).toBe(4);
  });
});
